



심볼 테이블에 대한 개념은 cs지식 -> 어셈블리어 -> 테이블 파트에서 다뤘다.


거기에 나오는 심볼 테이블과 비슷하지만 파이썬에서 말하는 심볼 테이블은

조금 다른 역할을 한다고 볼 수 있다.


파이썬의 심볼 테이블은 각 객체마다 변수와 레퍼런스값을 저장하기 위해 존재하는데.

각 객체마다 존재하는 이 심볼 테이블 덕분에 네임 스페이스가 형성이 될 수가 있다.

서로 같은 심볼 테이블에 포함되고 있다면 같은 네임 스페이스를 갖게 된다는 것이다.

그래서 다른 모듈이나 함수 클래스 등 같은 이름을 가진 변수와 함수에 대해

문제 없이 사용할 수 있는 것이다.