
초기화란 변수 선언과 동시에 값을 입력해주는 것.

ex)

int a = 10 이렇게 int a를 선언함과 동시에 10이라는 값을 입력해주는 걸 '초기화'라고 함.

초기화를 안 해주면 변수에는 '쓰레기 값'이 들어가 있는 상태가 유지 됨.

int a;

a = 10

이렇게만 해 놓으면 a는 '쓰레기 값'이 들어있게 되니 각종 버그와 오류를 유발함.

그러니 밑에 a = 10 처럼 따로 값을 지정해 주어야 함. 

하지만 이건 '초기화'라고 부르지는 않음





