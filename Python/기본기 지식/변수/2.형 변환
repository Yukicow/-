
형 변환

int()

float()

bool() #불리안 -> true 와 false 로 출력하는 변수.

str()


이 때 문자형 소수점 숫자를 int 형으로 변환시켜 줄 때에는 float()로 한 번 변경하고 해줘야 함.

ex)

'2.5'를 정수형으로 바꾸기 위해서는 int(float('2.5')) 이렇게 문자형을 실수형으로 고치고 정수형으로 가야 고쳐짐
출력하면 정수형이 될 때에는 소수점이 아예 날라가기 때문에 2로 출력 됨.