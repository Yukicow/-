
패키지


패키지는 모듈을 모아 놓은 것이라고 생각하면 됨.

모듈이 파일이라면 패키지가 파일.


결론)

패키지는 모듈을 모아 놓은 파일!



너무 뭐가 없으니까 내용 하나만 추가 하자.

import를 통해 패키지 안에 있는 모듈을 불러올 때에는

import 패키지.모듈 이렇게 불러오면 됨.

import를 통해 무언가 불러올 때에는 모듈이나 패키지만 가능하고

안에 있는 클래스나 함수를 직접 불러오는 건 불가능 함.

ex)

import 패키지.모듈 (O)

import 패키지.모듈.클래스.함수 (X)


하지만 from import는 가능 

from import 에서는 import 뒤에 모듈, 클래스, 함수 다 가능하니까.


